# Archived Prototypes

This folder preserves older one-file prototypes that informed the RainTech GRC platform direction.

## old-grc-platform-perplexity-index.html

Source:

`C:\Users\johnathan\AppData\Local\Temp\b821b007-ecdd-4b73-afc2-6258db6a53cf_GRC Platform.zip.GRC Platform.zip\index.html`

Notes:

- This is the older CMMC GRC Platform prototype shell.
- It is a static HTML file, but it expects backend routes such as `/api/controls`, `/api/library/...`, `/api/onboarding/...`, and `/api/assistant/...`.
- It should be treated as reference material, not the current running platform.
- The current local platform is served from the FastAPI app at `/platform`.
